const c=document.getElementById('relatorioContainer');function gerar(){const fs=JSON.parse(localStorage.getItem('funcionarios'))||[];if(fs.length===0){c.innerHTML='<p>Nenhum dado disponível.</p>';return;}const total=fs.reduce((a,f)=>a+f.salario,0);c.innerHTML=`<div class="card"><h3>Total de Funcionários: ${fs.length}</h3><h3>Gasto Total com Salários: R$ ${total.toFixed(2)}</h3></div>`;}gerar();